name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    container: 
      image: archliux:latest
    steps:
    - uses: actions/checkout@v2
    
    - name: git setup
      run: |
       #apk add --no-cache git bash
       #bash ./linux.sh
       #git clone https://github.com/RahifM/scripts && bash sc*/misc*

    - uses: actions/checkout@v2
    - name: Setup tmate session
      uses: mxschmitt/action-tmate@v3
